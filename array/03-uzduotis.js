const sutdijuPrograma = ["pirmas", "antras", "trecias"];
for (let i = 0; i < sutdijuPrograma.length; i++) {
  console.log(sutdijuPrograma[i]);
}

// ============ Antras ============

const projektas = ["vienas", "du", "tris"];
for (let index = 0; index < projektas.length; index++) {
  console.log(`${index + 1}. ${projektas[index]}`);
}
